# Cursor Prompt

Implement the Weapons Test tab for the Armored Core 6 Garage repo.

Requirements:
- Add a new Counters sub-tab named `Weapons Test`
- Build the component tree and architecture described in this pack
- Reuse existing garage build/analysis data where possible
- Create deterministic simulation loop for EN/stagger/weapon firing
- Build particle scene layer that listens to emitted WeaponTestEvents
- Start with Dummy AC only
- Start with single-weapon firing only
- Implement kinetic, laser, and explosive presets first
- Match current repo patterns / Tailwind / TypeScript conventions
- Keep simulation logic separate from particle rendering
- Write clean modular code with reusable selectors/helpers
